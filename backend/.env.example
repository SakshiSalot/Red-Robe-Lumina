# ===========================================
# Lumina Backend (Node/Express/MongoDB) config
# Copy this to backend/.env and fill in real values
# ===========================================

# Port the Express server listens on
PORT=8080

# MongoDB Atlas connection string
# Get this from Atlas: Cluster -> Connect -> Drivers
MONGODB_URI=mongodb+srv://sakshisalot04_db_user:xCtsm3OtOpiMJGjR@cluster0.rqkgx3a.mongodb.net/?appName=Cluster0

# JWT signing secret (use a long random string in production)
JWT_SECRET=uy9NJOHNsW7viaAnoY0uJnEnt4iERdNA5ic4KaX2TmY
# Token lifetime in milliseconds (default 24h, matches old Spring config)
JWT_EXPIRATION_MS=86400000

# Python AI engine base URL (unchanged from the old setup)
PYTHON_AI_URL=https://ai-assessment-platform.onrender.com

# Allowed CORS origins (comma-separated). Vercel + localhost dev.
# A value of "*" allows all origins.
FRONTEND_URL=https://devscore-ai.vercel.app
